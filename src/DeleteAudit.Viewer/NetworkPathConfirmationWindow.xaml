<Window x:Class="DeleteAudit.Viewer.NetworkPathConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="确认从网络共享导入"
        Width="600"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <StackPanel Margin="20">
        <TextBlock Text="你选择的是网络共享中的文件"
                   FontSize="15"
                   FontWeight="Bold"
                   Margin="0,0,0,10" />
        <TextBlock x:Name="NetworkPathText"
                   TextWrapping="Wrap"
                   FontFamily="Consolas"
                   Margin="0,0,0,14" />
        <TextBlock TextWrapping="Wrap" Margin="0,0,0,8">
            · 在系统文件选择器中浏览、输入或选择此位置时，Windows 可能已经连接该共享并检查路径或文件。这是系统选择器的行为，不代表 DeleteAudit 已经开始导入。
        </TextBlock>
        <TextBlock TextWrapping="Wrap" Margin="0,0,0,8">
            · Windows 可能使用当前登录凭据协商访问；DeleteAudit 不保存、也不向你索要网络凭据。
        </TextBlock>
        <TextBlock TextWrapping="Wrap" Margin="0,0,0,8">
            · 如果继续，DeleteAudit 将开始检查、打开并读取所选文件。
        </TextBlock>
        <TextBlock TextWrapping="Wrap" Margin="0,0,0,8">
            · 网络不可用或共享无响应时，导入可能失败或长时间无响应。
        </TextBlock>
        <TextBlock TextWrapping="Wrap" Margin="0,0,0,8">
            · 只有信任该位置时才继续。建议先复制到本机再导入。
        </TextBlock>
        <TextBlock TextWrapping="Wrap" Margin="0,0,0,16">
            · 取消会阻止 DeleteAudit 后续读取和导入，但不能撤销 Windows 文件选择器此前可能发生的访问。
        </TextBlock>
        <TextBlock TextWrapping="Wrap"
                   Margin="0,0,0,16"
                   Opacity="0.75">
            确认只对本次导入、当前路径有效，不会被记住。
        </TextBlock>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消"
                    IsDefault="True"
                    IsCancel="True"
                    MinWidth="110"
                    Padding="10,4"
                    Margin="0,0,10,0"
                    Click="OnCancelClick" />
            <Button Content="继续导入"
                    MinWidth="110"
                    Padding="10,4"
                    Click="OnContinueClick" />
        </StackPanel>
    </StackPanel>
</Window>
